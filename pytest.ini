# For one pytest session over both suites — an IDE's test explorer, `pytest core/tests runtime/tests`.
# Both are packages named `tests`, which pytest's default import mode cannot tell apart. Each
# suite's own command stays `python -m unittest discover -s tests` (core/README.md, runtime/README.md).
[pytest]
addopts = --import-mode=importlib
